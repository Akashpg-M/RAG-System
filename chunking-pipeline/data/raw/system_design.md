## Architecture
The system is built on Python. It uses embeddings to map semantic meaning.
AI pipelines require chunking, metadata enrichment, tokenization,
semantic search optimization, and vector indexing.

## Deployment
We deploy using Docker and Kubernetes to ensure high availability.
Distributed workers process ingestion tasks asynchronously.